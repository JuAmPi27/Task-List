// acá vamos a manejar esa base de datos creada en mock-tasks.ts
// se va a usar como interface


export interface task {
    id:number;
    texto:string;
    dia:string;
    recordatorio:boolean;
}

